from app.utilities.settings import cli, the


def sumOfTwoNumbers(num1, num2):
    return num1 + num2

# the = cli(the)
the={"nums":1000}

"""
-- ---------------------------------
--  Start up
the = cli(the)  
runs(the.eg)
rogues() 
os.exit(fails) 
"""
# TODO: Write above code in python